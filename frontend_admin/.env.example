# Frontend Admin (Vite)
# Copy file này thành .env nếu cần cấu hình riêng.

# Base URL cho backend (axios client / các request chính)
VITE_BACKEND_URL=http://localhost:5000

# Một số module đọc biến VITE_API_URL (sẽ tự thêm /api)
VITE_API_URL=http://localhost:5000

# Một số module packageApi đang đọc VITE_API_BASE_URL
# Khuyến nghị trỏ về backend 5000 để đồng bộ
VITE_API_BASE_URL=http://localhost:5000

# URL trang login của frontend_user để admin redirect khi logout/unauth
VITE_USER_LOGIN_URL=http://localhost:3000/login
